method div(a, b) {
    show(a)
    show(b)
    risk {
        when a>b {
            escape;
        }
    }
    clean {show(a*b)}
}


risk {
    show([1,2]-[3])
} trap IllegalOperationError e {
    show("Caught expected error:"+ e)
}



x,y=9,0
div(x,y)